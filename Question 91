#include<stdio.h>
int main(){
    char string[] = "This is an example string";
    for(int i = 0; i < sizeof(string);i++){
        if((64<string[i] && string[i]<70) || (96<string[i] && string > 102)){
            for(int i = 0; i < sizeof(string) ; i++){
                string[i] = string[i+1];
            }
        }
    }

    return 0;
}
