#include<stdio.h>
#include<string.h>
int main(){
    char longest[20] = "",string[] = "Aim is to find the longest word in a sentence ";
    int length = 0 ,idx = 0;
    for(int i = 0;i <= strlen(string);i++){
        if(string[i] == ' '){
        if(length > strlen(longest)){
            strncpy(longest,string + idx,length);
            longest[length] = '\0';
            
        }
        length = 0;
        idx = i + 1;
    }else{
         length++;
    }
    }
    printf("longest word in this sentence is %s",longest);
    return 0;
}
