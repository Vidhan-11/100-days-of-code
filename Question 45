#include<stdio.h>
int main(){
    int n,sum =0;
    printf("Number : \n");
    scanf("%d",&n);
    for(int i =1;i <= n; i++){
        sum += (2*i)/(3*i + i - 1);
    }
    printf("Sum to nth term for this series is : %d",sum);
    return 0;
}
