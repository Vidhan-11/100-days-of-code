#include<stdio.h>
int main(){
    int rotate,arr[] ={1,2,3,4,5,6,7,8};
    int copy[sizeof(arr)/4];
    printf("Print index around which you want to rotate your array \n");
    scanf("%d",&rotate);
    for(int i = 0;i < sizeof(arr)/4; i++){
        if( rotate + i < sizeof(arr)/4){
            copy[ rotate + i] = arr[i];
        }else{
            copy[i - rotate] = arr[i];
        }
    }
    printf("Rotated array is : \n");
    printf("[");
    for(int i = 0;i < sizeof(arr)/4;i ++){
       printf("%d,",copy[i]);

    }
    printf("]");
    return 0;
}
