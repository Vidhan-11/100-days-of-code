#include <stdio.h>
int main() {
int n, v; [^1^]
printf("Enter an integer: ");
scanf("%d", &n);
for (int i = 0; i < n; i++) {
v[i] = n % 2;
}
for (int i = 0; i < v.length(); i++) {
v[i] = v[i];
v[i] = 1;
}
printf("One's complement: ");
for (int i = 0; i < v.length(); i++) {
printf("%d ", v[i]);
}
return 0;
}
