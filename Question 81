#include<stdio.h>
//this is for a diagonal matrix.
int main(){
    int length = 0;
    char name[10] = "Vidhan\0";
    for(int i = 0;i <10;i++){
        if(name[i] = '\0'){
            break;
        }
        length++;
    }
    printf("Length of this string is %d",length);

    return 0;
}
