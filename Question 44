#include<stdio.h>
int main(){
    int n,sum =0;
    printf("Number : \n");
    scanf("%d",&n);
    for(int i =1;i <= n; i++){
        if(i == 1)
        sum += 1;
        else
        sum += (2*i - 1)/(2*i);
    }
    printf("Sum to nth term for this series is : %d",sum);
    return 0;
}
