#include<stdio.h>
int main(){
	int matrix[2][2] = {{2,5},{8,6}};
	for(int i = 0;i < 2;i ++){
		for(int j = 0;j < 2;j ++){
			matrix[i][j] = matrix[i][j]^matrix[j][i];
			matrix[j][i] = matrix[i][j]^matrix[j][i];
			matrix[i][j] = matrix[i][j]^matrix[j][i];
		}
	}
	return 0;
}
