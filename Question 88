#include<stdio.h>
int main(){
    char string[] = "This is a string for change";
    for(int i = 0;i < sizeof(string);i++){
        if(string[i] == '\b'){
            string[i] = '-';
        }
    }

    return 0;
}
